apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: test-sorting-two-generators
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - name: app-two-gen-1
        appPath: two-gen-1
        cluster: in-cluster
        url: https://kubernetes.default.svc
      - name: app-two-gen-2
        appPath: two-gen-2
        cluster: in-cluster
        url: https://kubernetes.default.svc
  - git:
      repoURL: https://github.com/aali309/argocd-test-nested
      revision: HEAD
      directories:
      - path: apps/git-dir-3
  template:
    metadata:
      name: '{{name}}'
      namespace: openshift-gitops
      labels:
        appPath: '{{path}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/aali309/argocd-test-nested
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: '{{server}}'
        namespace: openshift-gitops
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
