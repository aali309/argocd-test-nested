apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: test-sorting-three-generators
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - name: app-three-gen-1
        appPath: three-gen-1
        cluster: in-cluster
        url: https://kubernetes.default.svc
      - name: app-three-gen-2
        appPath: three-gen-2
        cluster: in-cluster
        url: https://kubernetes.default.svc
  - git:
      repoURL: https://github.com/aali309/argocd-test-nested
      revision: HEAD
      directories:
      - path: apps/git-dir-4
  - clusterDecisionResource:
      configMapRef: argocd-apps
      labelSelector:
        matchLabels:
          argocd.argoproj.io/secret-type: cluster
      requeueAfterSeconds: 180
  template:
    metadata:
      name: '{{name}}'
      namespace: openshift-gitops
      labels:
        appPath: '{{path}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/aali309/argocd-test-nested
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: '{{server}}'
        namespace: openshift-gitops
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
